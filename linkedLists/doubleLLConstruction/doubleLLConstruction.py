'''
PROMPT  
Write a DoublyLinkedList class that has a head and a tail, both 
of which point to either a linked list Node or None/null. The 
class should support:
    - Setting the head and tail of the linked list
    - Inserting nodes before and after other nodes as well as at
      a given position
    - Removing given nodes and removing nodes with given values
    -Searching for nodes with given values


SAMPLE INPUT 

SAMPLE OUTPUT

'''

def something():
    return "Hello, world"
